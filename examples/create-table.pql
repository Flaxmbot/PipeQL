-- Create the notes table schema (SQLite target).
table notes [
  id int primary auto,
  title string not null,
  content string not null,
  category string default 'Personal',
  is_pinned int default 0,
  created_at timestamp default current_timestamp
]
